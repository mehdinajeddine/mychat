import { ObjectCustom, UUIDMetadataObject, GetChannelMembersParameters } from "pubnub";
declare type HookReturnValue = [
    UUIDMetadataObject<ObjectCustom>[],
    () => Promise<void>,
    () => void,
    number,
    Error
];
export declare const useChannelMembers: (options: GetChannelMembersParameters) => HookReturnValue;
export {};
