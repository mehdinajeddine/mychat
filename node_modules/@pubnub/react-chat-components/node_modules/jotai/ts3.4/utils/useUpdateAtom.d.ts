import { WritableAtom } from 'jotai';
import { Scope, SetAtom } from '../core/atom';
export declare function useUpdateAtom<Value, Update, Result extends void | Promise<void>>(anAtom: WritableAtom<Value, Update, Result>, scope?: Scope): SetAtom<Update, Result>;
